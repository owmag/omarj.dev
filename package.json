{
  "name": "store1",
  "version": "1.0.0",
  "private": true,
  "description": "Omar J — Design Engineer. Static portfolio site.",
  "author": "Omar J",
  "license": "UNLICENSED",
  "scripts": {
    "build": "node scripts/gen-favicons.cjs && echo 'Static site — no compile step'",
    "favicons": "node scripts/gen-favicons.cjs",
    "package:netlify": "rm -f netlify.zip && zip -r netlify.zip netlify.toml _redirects _headers *.html robots.txt sitemap.xml favicon.ico favicon.svg favicon-32.png apple-touch-icon.png styles.css *.js vendor/pretext media/projects BUSINESSCARD.webp -x '*.mov' -x '*.DS_Store' -x '*/.DS_Store'",
    "vendor:pretext": "rsync -a --delete node_modules/@chenglou/pretext/dist/ vendor/pretext/",
    "dev": "python3 -m http.server 3000 --bind 127.0.0.1",
    "dev:serve": "npx --yes serve . -l 3000",
    "dev:py": "python3 -m http.server 8765 --bind 127.0.0.1"
  },
  "dependencies": {
    "@chenglou/pretext": "^0.0.5"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "to-ico": "^1.1.5"
  }
}
