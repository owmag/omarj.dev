# Static site — build runs `scripts/gen-favicons.cjs`. Routing, security, and caching
# are defined in `_redirects` and `_headers` at the repo root (Netlify + Cloudflare Pages).

[build]
  command = "npm run build"
  publish = "."
