# Client-side routes (e.g. /contact, /bio) — must be last (single catch-all proxy).
# Apex / naked domain → https://www.omarj.dev — set in Cloudflare (Bulk Redirects or
# Single Redirect Rule); Pages _redirects cannot match by hostname.

/superfreak    /superfreak/index.html    200
/*    /index.html   200
